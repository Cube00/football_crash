/**
 * The still shown when animation is off — the same beach scene, with the
 * character already balancing the ball, baked into one image.
 */
export const STILL_BACKGROUND = "/assets/game_bg";

/** Placeholder grant count until the wallet API is wired up. */
export const FREE_BET_COUNT = 127;
