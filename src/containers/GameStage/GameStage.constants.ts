/**
 * The still shown when animation is off — the same beach scene, with the
 * character already balancing the ball, baked into one image.
 */
export const STILL_BACKGROUND = "/assets/game_bg.webp";

/** Its natural size, so the box is reserved before the file lands. */
export const STILL_BACKGROUND_SIZE = { width: 1006, height: 514 } as const;
