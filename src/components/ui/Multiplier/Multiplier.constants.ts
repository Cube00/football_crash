/** Upper bound on pills rendered. The CSS grid fits as many as the row holds
    at the current width and clips the rest, so this is not a per-breakpoint
    count — it just needs to cover the widest case. */
export const MAX_MULTIPLIERS = 15;
